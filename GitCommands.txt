Perfect 👍
Here’s a **complete but clean Git + GitHub command cheat sheet** — from **beginner → advanced**, organized so you can quickly revise before interviews or real work.

---

# 🔹 GIT vs GITHUB (1-line)

* **Git** → version control tool (local)
* **GitHub** → remote hosting platform for Git repositories

---

# 🧱 1. BASIC SETUP (One time)

```bash
git --version
git config --global user.name "Your Name"
git config --global user.email "you@email.com"
git config --list
```

---

# 📁 2. CREATE / CLONE REPOSITORY

### Initialize new repo (local)

```bash
git init
```

### Clone existing GitHub repo

```bash
git clone https://github.com/user/repo.git
```

---

# 📊 3. CHECK STATUS & HISTORY

```bash
git status
git log
git log --oneline
git log --stat
git show
```

---

# ➕ 4. STAGING (Add files)

```bash
git add file.txt
git add folder/
git add .
```

Unstage:

```bash
git restore --staged file.txt
```

---

# 💾 5. COMMIT

```bash
git commit -m "message"
git commit -am "message"   # tracked files only
```

Amend last commit:

```bash
git commit --amend
```

---

# 🔄 6. PUSH / PULL (Local ↔ GitHub)

### Add remote

```bash
git remote add origin https://github.com/user/repo.git
```

### View remote

```bash
git remote -v
```

### Push

```bash
git push
git push origin main
git push -u origin main
```

### Pull

```bash
git pull
git pull origin main
git pull --rebase
```

### Fetch (no merge)

```bash
git fetch
```

---

# 🌿 7. BRANCHING

```bash
git branch              # list branches
git branch new-branch
git checkout new-branch
git checkout -b new-branch
git switch main
```

Delete branch:

```bash
git branch -d branch-name
git branch -D branch-name
```

Push branch:

```bash
git push origin branch-name
```

---

# 🔀 8. MERGING & REBASE

Merge:

```bash
git merge branch-name
```

Rebase:

```bash
git rebase main
```

Abort merge:

```bash
git merge --abort
```

---

# 🧨 9. UNDO / RESET / REVERT (VERY IMPORTANT)

### Undo working directory changes

```bash
git restore file.txt
```

### Reset staged file

```bash
git reset file.txt
```

### Reset commits

```bash
git reset --soft HEAD~1
git reset --mixed HEAD~1
git reset --hard HEAD~1
```

### Revert commit (safe)

```bash
git revert commit_hash
```

---

# 🔍 10. DIFF & TRACE CHANGES

```bash
git diff
git diff --staged
git diff commit1 commit2
```

File history:

```bash
git log file.txt
```

Line by line:

```bash
git blame file.txt
git blame -L 10,20 file.txt
```

Search code change:

```bash
git log -S "text"
```

---

# 📂 11. FILE / FOLDER OPERATIONS

```bash
git rm file.txt
git rm -r folder/
git mv old.txt new.txt
```

---

# 🚫 12. .gitignore

```bash
touch .gitignore
```

Check ignored files:

```bash
git check-ignore -v file.txt
```

---

# 🧳 13. STASH (Temporary save)

```bash
git stash
git stash list
git stash apply
git stash pop
git stash drop
```

---

# 🏷️ 14. TAGS (Releases)

```bash
git tag v1.0
git tag
git push origin v1.0
git push --tags
```

---

# 🔐 15. AUTHENTICATION (GitHub)

GitHub uses **PAT (token)** instead of password.

Check credentials:

```bash
git config --global --list
```

---

# 🧠 16. DAILY WORKFLOW (REMEMBER THIS)

```bash
git status
git add .
git commit -m "message"
git pull --rebase
git push
```

---

# 🧪 17. ADVANCED / RARE BUT USEFUL

```bash
git reflog
git cherry-pick commit_hash
git clean -fd
git bisect
```

---

# 📌 INTERVIEW FAVORITES (MUST KNOW)

* `git add`
* `git commit`
* `git push`
* `git pull`
* `git merge`
* `git rebase`
* `git reset` vs `git revert`
* `git stash`
* `git blame`

---

If you want next:

* ✅ **Git commands explained with real-life analogy**
* ✅ **GitHub workflow (PR, fork, review)**
* ✅ **DevOps-focused Git usage**
* ✅ **One-page printable PDF**

Just tell me 👍
